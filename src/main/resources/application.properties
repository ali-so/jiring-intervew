spring.datasource.url=jdbc:mysql://localhost:3306/acc-jiring?useSSL=false&ServerTimeZone=UTC
spring.datasource.username=root
spring.datasource.password=root

#hibernate
spring.jpa.properties.hibernate.cache.use_second_level_cache=true
spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.ehcache.EhCacheRegionFactory
spring.jpa.properties.hibernate.generate_statistics=true
spring.jpa.properties.hibernate.cache.use_query_cache=true

spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=update

############# Actuator Configs
info.app.name=Jiring Interview Assignment
info.app.description=A demo app for Jiring!
info.app.version=1.0.0

# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list
#
management.endpoints.web.exposure.include=*

# Exclude individual endpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=health,info

jwt.secret=jiring-company
jwt.expirationDateInMs=99999999999999999